import React from 'react';
import { CurrencySelect } from './Fields/CurrencySelect';

/** Этап выбора типа валюты. */
export function Currency (): React.JSX.Element {
    return (
        <CurrencySelect />
    );
}

