/** Тип шага формы. */
export enum EStepperStep {
    /** Выбор валюты. */
    CURRENCY = 'CURRENCY',
    /** Выбор суммы. */
    AMOUNT = 'AMOUNT',
    /** Страница успешной операции. */
    SUCCESS = 'SUCCESS'
}